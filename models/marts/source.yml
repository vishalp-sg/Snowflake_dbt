version: 2

sources:
  - name: core
    database: TRACTOR_DB        # your Snowflake DB
    schema: TRACTOR_SCHEMA_CORE # where the table actually lives
    tables:
      - name: new_telemetry_events
